What is Docker ?
Docker is a technology used for devloping, shipping,running. it packages an application and all its dependencies like 
libraries, files and code into a single unit called an container.
